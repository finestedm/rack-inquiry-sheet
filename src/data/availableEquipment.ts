import { IEquipment } from "../features/interfaces";

 const availableEquipment = [
    'dock', 'wall', 'gate', 'storage area', 'racking area', 'charging stations', 'pickup station', 'unload station', 'mezzanine', 'starcase'
]

export default availableEquipment